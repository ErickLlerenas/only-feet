import { LoadingButton } from "@mui/lab";
import "./style.scss";

const Loader = () => {
	return (
		<LoadingButton loading variant="outlined" className="loader">
			.
		</LoadingButton>
	);
};
export default Loader;
